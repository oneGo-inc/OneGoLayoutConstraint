import XCTest
@testable import OneGoLayoutConstraint

final class OneGoLayoutConstraintTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(OneGoLayoutConstraint().text, "Hello, World!")
    }
}
